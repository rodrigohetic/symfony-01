{% extends 'base.html.twig' %}

{% block title %}New Quote
{% endblock %}

{% block body %}
    <h1>Create new Quote</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}

    <a href="{{ path('quotes') }}">back to list</a>
{% endblock %}